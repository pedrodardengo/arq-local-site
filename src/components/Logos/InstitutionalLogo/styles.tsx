import styled from 'styled-components'

export const LogoWrapper = styled.div`
	position: relative;
	width: 50%;
	display: flex;
	align-items: center;
	cursor: pointer;

	img {
		position: absolute;
	}
`
